{% extends 'base.html.twig' %}

{% block title %}Modifier Ressource{% endblock %}

{% block body %}
    <h1>Modifier une Ressource</h1>

    {{ include('ressource/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="row mb-3">
        <div class="col-lg-1">
            <a href="{{ path('ressource_index') }}"><button type="submit" class="btn btn-primary">Retour à la liste</button></a>
        </div>
        <div class="col-lg-2">
            {{ include('ressource/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
