{% extends 'base.html.twig' %}

{% block title %}EtablissementScolaire index{% endblock %}

{% block body %}
    <h1>EtablissementScolaire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for etablissement_scolaire in etablissement_scolaires %}
            <tr>
                <td>{{ etablissement_scolaire.id }}</td>
                <td>{{ etablissement_scolaire.name }}</td>
                <td>
                    <a href="{{ path('etablissement_scolaire_show', {'id': etablissement_scolaire.id}) }}">show</a>
                    <a href="{{ path('etablissement_scolaire_edit', {'id': etablissement_scolaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('etablissement_scolaire_new') }}">Create new</a>
{% endblock %}
