{% extends 'base.html.twig' %}

{% block title %}EtablissementScolaire{% endblock %}

{% block body %}
    <h1>EtablissementScolaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etablissement_scolaire.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ etablissement_scolaire.name }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row mb-3">
        <div class="col-lg-1">
            <a href="{{ path('etablissement_scolaire_index') }}"><button class="btn btn-primary">Retour à la liste</button></a>
        </div>
        <div class="col-lg-1">
            <a href="{{ path('etablissement_scolaire_edit', {'id': etablissement_scolaire.id}) }}"><button class="btn btn-primary">Modifier</button></a>
        </div>
    </div>

    {{ include('etablissement_scolaire/_delete_form.html.twig') }}
{% endblock %}
