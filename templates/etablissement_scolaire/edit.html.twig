{% extends 'base.html.twig' %}

{% block title %}Edit EtablissementScolaire{% endblock %}

{% block body %}
    <div class="row m-3">
        <h1>Modifier un Etablissement Scolaire</h1>
    </div>
    <div class="row m-3">
        {{ include('etablissement_scolaire/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>
    <div class="row m-3">
        {{ include('etablissement_scolaire/_delete_form.html.twig') }}
    </div>
    <div class="row m-3">
        <a href="{{ path('etablissement_scolaire_index') }}"><button type="submit" class="btn btn-primary">Retour à la liste</button></a>
    </div>

{% endblock %}
