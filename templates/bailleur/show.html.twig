{% extends 'base.html.twig' %}

{% block title %}Bailleur{% endblock %}

{% block body %}
    <h1>Bailleur</h1>

    <div class="row m-3">
        <table class="table table-striped table-hover">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ bailleur.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ bailleur.name }}</td>
                </tr>
            </tbody>
        </table>
    </div>

    <div class="row m-3">
        <div class="col-6">
            <a href="{{ path('bailleur_index') }}">retour à la liste</a>
        </div>
        <div class="col-6">
            <a href="{{ path('bailleur_edit', {'id': bailleur.id}) }}">Modifier</a>
        </div>
    </div>


    {{ include('bailleur/_delete_form.html.twig') }}
{% endblock %}
