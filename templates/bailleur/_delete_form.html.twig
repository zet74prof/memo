<form method="post" action="{{ path('bailleur_delete', {'id': bailleur.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bailleur.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
