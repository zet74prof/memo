<form method="post" action="{{ path('type_prescripteur_delete', {'id': type_prescripteur.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_prescripteur.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
