{% extends 'base.html.twig' %}

{% block title %}TypePrescripteur index{% endblock %}

{% block body %}
    <h1>TypePrescripteur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_prescripteur in type_prescripteurs %}
            <tr>
                <td>{{ type_prescripteur.id }}</td>
                <td>{{ type_prescripteur.name }}</td>
                <td>
                    <a href="{{ path('type_prescripteur_show', {'id': type_prescripteur.id}) }}">show</a>
                    <a href="{{ path('type_prescripteur_edit', {'id': type_prescripteur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('type_prescripteur_new') }}">Create new</a>
{% endblock %}
