{% extends 'base.html.twig' %}

{% block title %}TypePrescripteur{% endblock %}

{% block body %}
    <h1>TypePrescripteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_prescripteur.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_prescripteur.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_prescripteur_index') }}">back to list</a>

    <a href="{{ path('type_prescripteur_edit', {'id': type_prescripteur.id}) }}">edit</a>

    {{ include('type_prescripteur/_delete_form.html.twig') }}
{% endblock %}
