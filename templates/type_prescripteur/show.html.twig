{% extends 'base.html.twig' %}

{% block title %}TypePrescripteur{% endblock %}

{% block body %}
    <h1>TypePrescripteur</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_prescripteur.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_prescripteur.name }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row mb-3">
        <div class="col-lg-2">
            <a href="{{ path('type_prescripteur_index') }}"><button class="btn btn-primary">Retour à la liste</button></a>
        </div>
        <div class="col-lg-2">
            <a href="{{ path('type_prescripteur_edit', {'id': type_prescripteur.id}) }}"><button class="btn btn-primary">Modifier</button></a>
        </div>
    </div>
    {{ include('type_prescripteur/_delete_form.html.twig') }}
{% endblock %}
