{% extends 'base.html.twig' %}

{% block title %}Territoire{% endblock %}

{% block body %}
    <h1>Territoire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ territoire.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ territoire.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('territoire_index') }}">back to list</a>

    <a href="{{ path('territoire_edit', {'id': territoire.id}) }}">edit</a>

    {{ include('territoire/_delete_form.html.twig') }}
{% endblock %}
