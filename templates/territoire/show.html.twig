{% extends 'base.html.twig' %}

{% block title %}Territoire{% endblock %}

{% block body %}
    <h1>Territoire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ territoire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ territoire.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('territoire_index') }}">retour à la liste</a>

    <a href="{{ path('territoire_edit', {'id': territoire.id}) }}">Modifier</a>

    {{ include('territoire/_delete_form.html.twig') }}
{% endblock %}
