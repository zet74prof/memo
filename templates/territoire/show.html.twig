{% extends 'base.html.twig' %}

{% block title %}Territoire{% endblock %}

{% block body %}
    <h1>Territoire</h1>

    <div class="row m-3">
        <table class="table table-striped table-hover">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ territoire.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ territoire.name }}</td>
                </tr>
            </tbody>
        </table>

        <div class="row mb-3">
            <div class="col-lg-2">
                <a href="{{ path('territoire_index') }}"><button class="btn btn-primary">Retour à la liste</button></a>
            </div>
            <div class="col-lg-2">
                <a href="{{ path('territoire_edit', {'id': territoire.id}) }}"><button class="btn btn-primary">Modifier</button></a>
            </div>
        </div>

        {{ include('territoire/_delete_form.html.twig') }}
    </div>
{% endblock %}
