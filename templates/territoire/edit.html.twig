{% extends 'base.html.twig' %}

{% block title %}Edit Territoire{% endblock %}

{% block body %}
    <h1>Edit Territoire</h1>

    {{ include('territoire/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="row mb-3">
        <div class="col-lg-1">
            <a href="{{ path('territoire_index') }}"><button type="submit" class="btn btn-primary">Retour à la liste</button></a>
        </div>
        <div class="col-lg-2">
            {{ include('territoire/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
