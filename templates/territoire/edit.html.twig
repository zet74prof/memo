{% extends 'base.html.twig' %}

{% block title %}Modifier Territoire{% endblock %}

{% block body %}
    <div class="row m-3">
        <h1>Modifier un Territoire</h1>
    </div>
    <div class="row m-3">
        {{ include('territoire/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>
    <div class="row m-3">
        {{ include('territoire/_delete_form.html.twig') }}
    </div>
    <div class="row m-3">
        <a href="{{ path('territoire_index') }}"><button type="submit" class="btn btn-primary">Retour à la liste</button></a>
    </div>

{% endblock %}
