<form method="post" action="{{ path('state_histo_delete', {'id': state_histo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ state_histo.id) }}">
    <button class="btn">Delete</button>
</form>
