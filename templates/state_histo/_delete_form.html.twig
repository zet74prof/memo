<form method="post" action="{{ path('state_histo_delete', {'id': state_histo.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ state_histo.id) }}">
    <button class="btn">Supprimer</button>
</form>
