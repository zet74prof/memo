{% extends 'base.html.twig' %}

{% block title %}StateHisto{% endblock %}

{% block body %}
    <h1>StateHisto</h1>

    <div class="row m-3">
        <table class="table table-striped table-hover">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ state_histo.id }}</td>
                </tr>
                <tr>
                    <th>Date</th>
                    <td>{{ state_histo.date ? state_histo.date|date('Y-m-d') : '' }}</td>
                </tr>
                <tr>
                    <th>Active</th>
                    <td>{{ state_histo.active ? 'Yes' : 'No' }}</td>
                </tr>
                <tr>
                    <th>Reason</th>
                    <td>{{ state_histo.reason }}</td>
                </tr>
            </tbody>
        </table>

        <div class="row mb-3">
            <div class="col-lg-1">
                <a href="{{ path('state_histo_index') }}"><button class="btn btn-primary">Retour à la liste</button></a>
            </div>
            <div class="col-lg-1">
                <a href="{{ path('state_histo_edit', {'id': state_histo.id}) }}"><button class="btn btn-primary">Modifier</button></a>
            </div>
        </div>

        {{ include('state_histo/_delete_form.html.twig') }}
    </div>
{% endblock %}
