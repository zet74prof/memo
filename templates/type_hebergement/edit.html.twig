{% extends 'base.html.twig' %}

{% block title %} Modifier type d'hébergement{% endblock %}

{% block body %}
    <div class="row m-3">
        <h1>Modifier un type d'hébergement</h1>
    </div>
    <div class="row m-3">
        {{ include('type_hebergement/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>
    <div class="row m-3">
        {{ include('type_hebergement/_delete_form.html.twig') }}
    </div>
    <div class="row m-3">
        <a href="{{ path('type_hebergement_index') }}"><button type="submit" class="btn btn-primary">Retour à la liste</button></a>
    </div>

{% endblock %}
