{% extends 'base.html.twig' %}

{% block title %}TypeHebergement index{% endblock %}

{% block body %}
    <h1>TypeHebergement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_hebergement in type_hebergements %}
            <tr>
                <td>{{ type_hebergement.id }}</td>
                <td>{{ type_hebergement.name }}</td>
                <td>
                    <a href="{{ path('type_hebergement_show', {'id': type_hebergement.id}) }}">show</a>
                    <a href="{{ path('type_hebergement_edit', {'id': type_hebergement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('type_hebergement_new') }}">Create new</a>
{% endblock %}
