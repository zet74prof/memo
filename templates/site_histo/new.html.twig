{% extends 'base.html.twig' %}

{% block title %}New SiteHisto{% endblock %}

{% block body %}
    <h1>Créer un SiteHisto</h1>

    {{ include('site_histo/_form.html.twig') }}

    <div class="row mb-3">
        <div class="col-lg-2">
            <a href="{{ path('site_histo_index') }}"><button class="btn btn-primary">Retour à la liste</button></a>
        </div>
    </div>
{% endblock %}
