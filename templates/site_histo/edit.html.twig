{% extends 'base.html.twig' %}

{% block title %}Edit SiteHisto{% endblock %}

{% block body %}
    <h1>Edit SiteHisto</h1>

    {{ include('site_histo/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="row mb-3">
        <div class="col-lg-1">
            <a href="{{ path('site_histo_index') }}"><button type="submit" class="btn btn-primary">Retour à la liste</button></a>
        </div>
        <div class="col-lg-2">
            {{ include('site_histo/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
