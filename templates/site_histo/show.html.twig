{% extends 'base.html.twig' %}

{% block title %}SiteHisto{% endblock %}

{% block body %}
    <h1>SiteHisto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ site_histo.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ site_histo.date ? site_histo.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('site_histo_index') }}">retour à la list</a>

    <a href="{{ path('site_histo_edit', {'id': site_histo.id}) }}">modifier</a>

    {{ include('site_histo/_delete_form.html.twig') }}
{% endblock %}
