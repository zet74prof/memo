{% extends 'base.html.twig' %}

{% block title %}SiteHisto{% endblock %}

{% block body %}
    <h1>SiteHisto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ site_histo.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ site_histo.date ? site_histo.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row mb-3">
        <div class="col-lg-1">
            <a href="{{ path('site_histo_index') }}"><button class="btn btn-primary">Retour à la liste</button></a>
        </div>
        <div class="col-lg-1">
            <a href="{{ path('site_histo_edit', {'id': site_histo.id}) }}"><button class="btn btn-primary">Modifier</button></a>
        </div>
    </div>

    {{ include('site_histo/_delete_form.html.twig') }}
{% endblock %}
