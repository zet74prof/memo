<form method="post" action="{{ path('site_histo_delete', {'id': site_histo.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ site_histo.id) }}">
    <button class="btn">Supprimer</button>
</form>
