<form method="post" action="{{ path('site_histo_delete', {'id': site_histo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ site_histo.id) }}">
    <button class="btn">Delete</button>
</form>
