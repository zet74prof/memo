{% extends 'base.html.twig' %}

{% block title %}Edit SituationFamiliale{% endblock %}

{% block body %}
    <div class="row m-3">
        <h1>Edit SituationFamiliale</h1>
    </div>

    {{ include('situation_familiale/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="row mb-3">
        <div class="col-lg-2">
            <a href="{{ path('situation_familiale_index') }}"><button type="submit" class="btn btn-primary">Retour à la liste</button></a>
        </div>
        <div class="col-lg-2">
            {{ include('situation_familiale/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
