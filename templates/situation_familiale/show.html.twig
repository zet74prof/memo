{% extends 'base.html.twig' %}

{% block title %}SituationFamiliale{% endblock %}

{% block body %}
    <h1>SituationFamiliale</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ situation_familiale.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ situation_familiale.name }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row mb-3">
        <div class="col-lg-2">
            <a href="{{ path('situation_familiale_index') }}"><button class="btn btn-primary">Retour à la liste</button></a>
        </div>
        <div class="col-lg-2">
            <a href="{{ path('situation_familiale_edit', {'id': situation_familiale.id}) }}"><button class="btn btn-primary">Modifier</button></a>
        </div>
    </div>

    {{ include('situation_familiale/_delete_form.html.twig') }}
{% endblock %}
