{% extends 'base.html.twig' %}

{% block title %}Modifier Statut{% endblock %}

{% block body %}
    <div class="row m-3">
        <h1>Modifier un statut</h1>
    </div>
    <div class="row m-3">
        {{ include('status/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>
    <div class="row m-3">
        <a href="{{ path('status_index') }}"><button type="submit" class="btn btn-primary">Retour à la liste</button></a>
    </div>

{% endblock %}
