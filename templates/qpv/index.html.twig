{% extends 'base.html.twig' %}

{% block title %}QPV index{% endblock %}

{% block body %}
    <h1>QPV index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>QpvName</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for q_p_v in q_p_vs %}
            <tr>
                <td>{{ q_p_v.id }}</td>
                <td>{{ q_p_v.qpvName }}</td>
                <td>
                    <a href="{{ path('q_p_v_show', {'id': q_p_v.id}) }}">show</a>
                    <a href="{{ path('q_p_v_edit', {'id': q_p_v.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('q_p_v_new') }}">Create new</a>
{% endblock %}
