{% extends 'base.html.twig' %}

{% block title %}QPV{% endblock %}

{% block body %}
    <h1>QPV</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ q_p_v.id }}</td>
            </tr>
            <tr>
                <th>Nom du QPV</th>
                <td>{{ q_p_v.qpvName }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row mb-3">
        <div class="col-lg-1">
            <a href="{{ path('q_p_v_index') }}"><button class="btn btn-primary">Retour à la liste</button></a>
        </div>
        <div class="col-lg-1">
            <a href="{{ path('q_p_v_edit', {'id': q_p_v.id}) }}"><button class="btn btn-primary">Modifier</button></a>
        </div>
    </div>

    {{ include('qpv/_delete_form.html.twig') }}
{% endblock %}
