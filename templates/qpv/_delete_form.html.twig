<form method="post" action="{{ path('q_p_v_delete', {'id': q_p_v.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ q_p_v.id) }}">
    <button class="btn">Delete</button>
</form>
