{% extends 'base.html.twig' %}

{% block title %}Modifier QPV{% endblock %}

{% block body %}
    <div class="row m-3">
        <h1>Modifier un QPV</h1>
    </div>

    {{ include('qpv/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="row m-3">
        <a href="{{ path('q_p_v_index') }}"><button type="submit" class="btn btn-primary">Retour à la liste</button></a>
    </div>

{% endblock %}
