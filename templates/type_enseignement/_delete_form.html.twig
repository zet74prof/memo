<form method="post" action="{{ path('type_enseignement_delete', {'id': type_enseignement.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_enseignement.id) }}">
    <div class="col-lg-1">
        <button class="btn btn-warning btn-xs">Supprimer</button>
    </div>
</form>
