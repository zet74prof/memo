<form method="post" action="{{ path('type_enseignement_delete', {'id': type_enseignement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_enseignement.id) }}">
    <button class="btn">Delete</button>
</form>
