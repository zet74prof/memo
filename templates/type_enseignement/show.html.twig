{% extends 'base.html.twig' %}

{% block title %}TypeEnseignement{% endblock %}

{% block body %}
    <h1>TypeEnseignement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_enseignement.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_enseignement.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_enseignement_index') }}">back to list</a>

    <a href="{{ path('type_enseignement_edit', {'id': type_enseignement.id}) }}">edit</a>

    {{ include('type_enseignement/_delete_form.html.twig') }}
{% endblock %}
