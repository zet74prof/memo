{% extends 'base.html.twig' %}

{% block title %}TypeEnseignement{% endblock %}

{% block body %}
    <h1>TypeEnseignement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_enseignement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_enseignement.name }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row mb-3">
        <div class="col-lg-1">
            <a href="{{ path('type_enseignement_index') }}"><button class="btn btn-primary">Retour à la liste</button></a>
        </div>
        <div class="col-lg-1">
            <a href="{{ path('type_enseignement_edit', {'id': type_enseignement.id}) }}"><button class="btn btn-primary">Modifier</button></a>
        </div>
    </div>

    {{ include('type_enseignement/_delete_form.html.twig') }}
{% endblock %}
