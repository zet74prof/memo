<form method="post" action="{{ path('benevole_delete', {'id': benevole.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ benevole.id) }}">
    <button class="btn">Delete</button>
</form>
