{% extends 'base.html.twig' %}

{% block title %}Nouveau niveau de formation{% endblock %}

{% block body %}
    <div class="row m-3">
        <h1>Créer un Niveau de Formation</h1>
    </div>
    <div class="row m-3">
        {{ include('niveau_formation/_form.html.twig') }}
    </div>
    <div class="row m-3">
        <a href="{{ path('niveau_formation_index') }}"><button class="btn btn-primary">Retour à la liste</button></a>
    </div>
{% endblock %}
