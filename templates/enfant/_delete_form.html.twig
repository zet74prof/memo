<form method="post" action="{{ path('enfant_delete', {'id': enfant.id}) }}" onsubmit="return confirm('ête-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enfant.id) }}">
    <div class="col-lg-2">
        <button class="btn btn-danger btn-xs">Supprimer</button>
    </div>
</form>
