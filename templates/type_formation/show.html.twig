{% extends 'base.html.twig' %}

{% block title %}TypeFormation{% endblock %}

{% block body %}
    <h1>TypeFormation</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_formation.id }}</td>
            </tr>
            <tr>
                <th>Nom Formation</th>
                <td>{{ type_formation.formationName }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row mb-3">
        <div class="col-lg-2">
            <a href="{{ path('type_formation_index') }}"><button class="btn btn-primary">Retour à la liste</button></a>
        </div>
        <div class="col-lg-2">
            <a href="{{ path('type_formation_edit', {'id': type_formation.id}) }}"><button class="btn btn-primary">Modifier</button></a>
        </div>
    </div>

    {{ include('type_formation/_delete_form.html.twig') }}
{% endblock %}
