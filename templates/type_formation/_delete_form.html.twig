<form method="post" action="{{ path('type_formation_delete', {'id': type_formation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_formation.id) }}">
    <button class="btn">Delete</button>
</form>
