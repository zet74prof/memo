{% extends 'base.html.twig' %}

{% block title %}Edit TypeFormation{% endblock %}

{% block body %}
    <div class="row m-3">
        <h1>Edit TypeFormation</h1>
    </div>

    {{ include('type_formation/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="row mb-3">
        <div class="col-lg-2">
            <a href="{{ path('type_formation_index') }}"><button type="submit" class="btn btn-primary">Retour à la liste</button></a>
        </div>
        <div class="col-lg-2">
            {{ include('type_formation/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
