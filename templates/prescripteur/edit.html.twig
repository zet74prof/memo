{% extends 'base.html.twig' %}

{% block title %}Edit Prescripteur{% endblock %}

{% block body %}
    <h1>Edit Prescripteur</h1>

    {{ include('prescripteur/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="row mb-3">
        <div class="col-lg-1">
            <a href="{{ path('prescripteur_index') }}">retour à la liste</a><button type="submit" class="btn btn-primary">Retour à la liste</button></a>
        </div>
        <div class="col-lg-2">
            {{ include('prescripteur/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
