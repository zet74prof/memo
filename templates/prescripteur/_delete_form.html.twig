<form method="post" action="{{ path('prescripteur_delete', {'id': prescripteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prescripteur.id) }}">
    <button class="btn">Delete</button>
</form>
