{% extends 'base.html.twig' %}

{% block title %}Apprenant{% endblock %}

{% block body %}
    <h1>Apprenant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ apprenant.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ apprenant.username }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ apprenant.roles ? apprenant.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ apprenant.password }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ apprenant.surname }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ apprenant.firstname }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ apprenant.title }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ apprenant.address }}</td>
            </tr>
            <tr>
                <th>PostalCode</th>
                <td>{{ apprenant.postalCode }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ apprenant.city }}</td>
            </tr>
            <tr>
                <th>BirthDate</th>
                <td>{{ apprenant.birthDate ? apprenant.birthDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Tel1</th>
                <td>{{ apprenant.tel1 }}</td>
            </tr>
            <tr>
                <th>Tel2</th>
                <td>{{ apprenant.tel2 }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ apprenant.comment }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ apprenant.email }}</td>
            </tr>
            <tr>
                <th>EnfantACharge</th>
                <td>{{ apprenant.enfantACharge }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('apprenant_index') }}">Retour à la liste</a>

    <a href="{{ path('apprenant_edit', {'id': apprenant.id}) }}">Modifier</a>

    {{ include('apprenant/_delete_form.html.twig') }}
{% endblock %}
