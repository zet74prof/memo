{% extends 'base.html.twig' %}

{% block title %}Modifier Apprenant{% endblock %}

{% block body %}
    <h1>Modifier un Apprenant</h1>

    {{ include('apprenant/_form.html.twig', {'button_label': 'Update'}) }}

<div class="row mb-3">
    <div class="col-lg-1">
        <a href="{{ path('apprenant_index') }}"><button type="submit" class="btn btn-primary">Retour à la liste</button></a>
    </div>
    <div class="col-lg-2">
        {{ include('apprenant/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
